filter:
    excluded_paths: [ tests/* ]

checks:
    php: true

build:
    nodes:
        coverage:
            tests:
                override:
                    -
                        command: vendor/bin/phpunit
                        coverage:
                            file: tmp/coverage.xml
                            format: clover

#tools:
#    external_code_coverage: true
